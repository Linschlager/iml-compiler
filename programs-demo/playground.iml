program Playground() // Just a playground to test the compiler
global
    record p(x: int32);
    const x: int32;

    fun wrap(x: int32) returns const r: p do
        r init := p(x)
    endfun
do
    debugin x init;
    debugout wrap(x)
endprogram
